import { createStore } from 'vuex'
import storage from '@/utils/storage'

export default createStore({
  state: () => {
    return {
      token: '123456'
    }
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
